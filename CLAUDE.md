# Claude Code Project Instructions

@AGENTS.md

Use the repository-root `AGENTS.md` as the single shared instruction source. Then follow the document routing in `docs/README.md` and the active work in `docs/PLAN.md`.

Do not treat `docs/research/` as an implementation instruction. Do not begin edits until the inspect-and-plan response has been approved, and stop at every approval gate defined in `AGENTS.md`.
